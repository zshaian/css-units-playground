import type { UnitState } from "@/types";

export const INITIAL_UNIT_STATE_VALUE: UnitState = {
  fromUnitValue: 16,
  fromUnit: "px",
  toUnitValue: 1,
  toUnit: "rem",
  rootFontSize: 16,
  baseFontSize: 16,
};
