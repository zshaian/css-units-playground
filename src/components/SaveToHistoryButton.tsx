import { Button } from "./ui/button";

const SaveToHistoryButton: React.FC = () => {
  return (
    <Button className="px-4 py-2 capitalize" variant="outline">
      save to history
    </Button>
  );
};

export default SaveToHistoryButton;
